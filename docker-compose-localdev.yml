version: '3.8'

services:
  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data

  ragchain:
    build:
      context: ./ragchain
    env_file:
      - ./ragchain/.env
    depends_on:
      - redis

  backend:
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "8080:8080"
    depends_on:
      - ragchain

volumes:
  redis_data:
